<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:android="http://schemas.android.com/apk/res/android">
    <item
            android:id="@+id/action_add_note"
            android:icon="@drawable/baseline_note_add_black_18dp"
            android:title="@string/add_note_item_menu"
            app:showAsAction="ifRoom"/>

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_exit"
          android:icon="@drawable/baseline_exit_to_app_black_18dp"
          android:title="@string/exit_item_menu"
          app:showAsAction="never"/>

    <item android:id="@+id/action_sort"
          android:icon="@drawable/baseline_sort_black_18dp"
          android:title="@string/sort_item_mene"
          app:showAsAction="ifRoom"/>

</menu>